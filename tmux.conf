## ENVIRONMENT ##
#{
#set -g default-terminal "screen-256color"
set -g history-limit 100000
set -g status-keys vi
# Use vim keybindings in copy mode
setw -g mode-keys vi
setw -g mouse on
# Base Index 0 is too far from left
set -g base-index 1
setw -g pane-base-index 1
## No delay for escape key press
#set -sg escape-time 0
# Source the Tmux configuration file
#bind r source-file ~/.tmux.conf \; display '~/.tmux.conf sourced'
#}

## MACOS PBCOPY ##
# https://thoughtbot.com/blog/tmux-copy-paste-on-os-x-a-better-future
# brew install reattach-to-user-namespace
# USAGE: C-b [, then y Enter to copy and exit copy-mode
#{
bind-key -T copy-mode-vi y send-keys -X copy-pipe "reattach-to-user-namespace pbcopy"
#}

## STATUS BAR ##
#{
set -g status-position bottom
set -g status-bg colour234
set -g status-fg colour137
set -g status-left-length 20
set -g status-left '#[fg=yellow]#H:#[fg=green]#S '
set -g status-right-length 50
set -g status-right '#{?pane_synchronized,#[fg=green]SYNC, } #[fg=yellow]#(cut -d " " -f 1-3 /proc/loadavg)#[default] #[fg=white]#(date -u)#[default]'
#set -g status-justify left
#set -g status-interval 5
# Window label
setw -g window-status-current-style bg=colour5
setw -g window-status-current-format ' #I#[fg=colour250]:#[fg=colour255]#W#[fg=colour50]#F '
setw -g window-status-format ' #I#[fg=colour237]:#[fg=colour250]#W#[fg=colour244]#F '
#}

## WINDOWS ##
#{
# Window activity notification
setw -g monitor-activity on
# Display message about new activity
#set -g visual-activity on
#set -g visual-bell off
#set -g visual-silence off
#set -g bell-action none
# Shift arrow to swap windows
bind -n S-Up swap-window -t -1
bind -n S-Down swap-window -t +1
# Shift arrow for window selection
bind -n S-Left  previous-window
bind -n S-Right next-window
#}

## PANES ##
#{
# Toggle pane synchronization
bind-key S set-window-option synchronize-panes
# Increase pane display C-b q timeout to 5 seconds
set -g display-panes-time 5000
# Vim style pane selection
bind -r h select-pane -L
bind -r j select-pane -D
bind -r k select-pane -U
bind -r l select-pane -R
# Resize pane by 5
bind-key -r J resize-pane -D 5
bind-key -r K resize-pane -U 5
bind-key -r H resize-pane -L 5
bind-key -r L resize-pane -R 5
# Use Alt-vim keys with prefix key to resize panes ##DOES NOT WORK on MACOS for L/R
#bind-key M-j resize-pane -D
#bind-key M-k resize-pane -U
#bind-key M-h resize-pane -L
#bind-key M-l resize-pane -R
# Move panes
# Default prefix C-o/M-o already does this
#bind-key O swap-pane -t :.1 \; select-pane -t :.1
# Use Alt-vim keys without prefix key to switch panes ## DOES NOT WORK on MACOS
#bind -n M-h select-pane -L
#bind -n M-j select-pane -D
#bind -n M-k select-pane -U
#bind -n M-l select-pane -R
#}

## Tmux Plugin Manger ##
#{
# git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm
# prefix + I to initialize plugin
# prefix + U to update plugin
# prefix + alt + u to remove plugin after commenting out
set -g @plugin 'tmux-plugins/tpm'
set -g @plugin 'tmux-plugins/tmux-sensible'

## tmux-plugins
# prefix + Ctrl-s --> Saves
# prefix + Crtl-r --> Restores
set -g @plugin 'tmux-plugins/tmux-resurrect'
set -g @resurrect-capture-pane-contents 'on'
set -g @resurrect-strategy-vim 'session'
set -g @resurrect-processes 'ssh vi vim man less more tail top htop'

## Initialize TMUX plugin manager (keep this line at the very bottom of tmux.conf)
run '~/.tmux/plugins/tpm/tpm'
#}

##### NOTES ######################
# Move section above plugin to use
##################################
## TMUX COPY BUFFER TO MINNTY ##
#{
# vi ~/.minttyrc
# AllowSetSelection=true
#}
## WINDOW ##
#{
# Window Title
#set -g set-titles on
#set -g set-titles-string '#S:#I.#P #W'
# Split
#bind-key | split-window -h
#bind-key - split-window -v
# Vim style windows selection
#bind -r H previous-window
#bind -r L next-window
#}
## PANE ##
#{
# Select
# Use Alt-arrow keys without prefix key to switch panes
#bind -n M-Left select-pane -L
#bind -n M-Right select-pane -R
#bind -n M-Up select-pane -U
#bind -n M-Down select-pane -D
# Movement
##bind-key j command-prompt -p "join pane from :#"  "join-pane -s '%%'"
##bind-key t command-prompt -p "send pane to :#"  "join-pane -t '%%'"
#}
